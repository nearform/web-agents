{
  "name": "web-agents",
  "version": "0.0.1",
  "description": "Agents in the browser with WebMCP and Chrome Prompt API.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "npx serve -p 4610",
    "lint:fix": "eslint . --fix",
    "lint:check": "eslint . --quiet",
    "pretty:fix": "prettier . --write",
    "pretty:check": "prettier . --check",
    "check": "npm run lint:check && npm run pretty:check",
    "format": "npm run lint:fix && npm run pretty:fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1"
  }
}
